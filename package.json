{
  "name": "calculator-dom-assignment",
  "version": "1.0.0",
  "description": "A simple calculator implemented with clean separation between logic and DOM, fully tested using Jest and JSDOM.",
  "type": "commonjs",
  "scripts": {
    "test": "npx jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^22.1.0"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}